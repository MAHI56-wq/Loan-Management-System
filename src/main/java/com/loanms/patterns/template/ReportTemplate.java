package com.loanms.patterns.template; public abstract class ReportTemplate { public final String generate(){StringBuilder b=new StringBuilder();b.append(header()).append("\n").append(body()).append("\n").append(footer());return b.toString();} protected String header(){return "=== Loan Management Report ===";} protected abstract String body(); protected String footer(){return "=== End Report ===";} }
